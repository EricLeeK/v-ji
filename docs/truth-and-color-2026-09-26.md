# 真实数据与前两页配色验收

2026-09-26，延续现有薄荷绿界面与系统字体；页面修改尚未部署到 Vercel。

## 真实数据

- `books.learner_count` 现在统计拥有有效加入副本的独立账号，排除 `demo@huaji.local`。加入后尚未复习也计入；删除副本后不再计入。它表示当前加入人数，并非同时在线人数。
- 数据库事务内维护统计，按卡册加行锁；重复加入保持幂等，保留原先的课程标签、章节、布局与来源复制逻辑。同步处理成员删除、账号删除、副本来源变化和演示邮箱身份变化。
- 列表和详情在人数为 0 时隐藏，在 1 人及以上时显示精确人数。热门排序使用同一真实计数，并增加稳定的并列排序。
- 已在连接的 Supabase 中应用 `20260926120000_real_learner_counts.sql`、`20260926121000_label_sample_books.sql`。回读 5 本卡册的计数均为 0，与排除演示账号后的查询一致。
- 前 3 本启动示例移除“5500”等不实规模和虚构作者署名，标题、介绍明确标注示例；只同步修正演示账号中未自行改名的示例副本。后 2 本导入资料保留。
- 目录数量从实际笔记生成的卡片数计算；首页改为真实最近 7 天打卡，删除固定三分之一进度与假定的 18:00 / 20:00 计划。修正新卡重复计入已学总数、“本月目标”错写，以及不足一分钟的学习时长显示。

## 配色

- 今日页：复习用雾蓝、新卡用薄荷绿、时间与打卡用暖杏色，今日目标使用雾蓝。
- 卡片盒：柔和底色、同色图标和进度条；AI 制卡入口使用淡紫。卡盒颜色由 ID 固定，搜索和排序时保持稳定，近期卡盒使用相同颜色。
- 未添加外部字体、图片资源或动画依赖；保留现有按钮反馈和小屏字号。

## 验证

- 93 项单元测试通过，包含列表和详情对 0、1、1,200 人的真实渲染验证。
- 真实数据库回归先复现“演示账号被计入”的失败；迁移后事务回归通过，覆盖重复加入、多人加入、删除、重加、演示昵称变化、邮箱身份变化、来源解除/恢复、账号删除与元数据保留。事务全部回滚。
- 最终回读：测试账号、测试卡册、测试副本均为 0。
- 生产构建、TypeScript、差异空白检查通过；ESLint 0 错误，保留原有 4 条警告。
- 33 项浏览器检查通过，覆盖 320px / 390px / 1280px、零人数隐藏、目录实际数量、搜索颜色稳定、无横向溢出/标题截断、无浏览器异常。
- 新辅助色文字对比度最低 6.58:1。实测 4 倍 CPU 降速、断网后的卡盒筛选至绘制约 51ms，外部字体请求 0；该数值只代表这次本地测量。
- 截图和检查结果位于 `output/playwright/truth-color-*`，本次浏览器未写入业务数据。

迁移通过事务 SQL 应用，沿用当前项目的手动发布方式；未改写已有迁移登记差异。今后统一迁移记录时应连同此前手动发布的课程迁移一起核对。
