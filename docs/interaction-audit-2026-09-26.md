# V 记交互排查与修复 · 2026-09-26

本次针对「按钮点不动、点击没有反馈、学习页右上角与设置页」检查实际事件绑定、持久化、路由和失败恢复。改动在本地工作区，未发布到线上。

## 修复范围

| 页面 | 原问题 | 当前行为 |
| --- | --- | --- |
| 学习 | 切换、更多与收藏没有操作 | 切换把当前卡片移到本轮末尾且不计分；收藏可取消并持久化；更多提供编辑、暂停、学习设置和手势说明 |
| 学习朗读 | 没有播放状态，不支持时静默；挖空答案提前读出 | 播放 / 停止状态、切卡自动停止、失败提示；未揭示的目标挖空读作「空白」 |
| 学习反馈 | 顶部提示遮挡操作按钮，悬停阻止提示消失 | 提示移到工具栏下方，提示正文不拦截点击，保留可点击的提醒操作与关闭按钮 |
| 设置 | 参数缺少验证、保存失败可能一直等待、其他页面继续用旧参数 | 客户端与服务端校验；保留失败草稿、重试和防重复提交；保存刷新今日与学习设置；并发修改提示冲突 |
| 设置朗读 / 提醒 | 无试听，授权通知后误称每日提醒已生效 | 提供试听和音色状态；页面打开时到点提醒、按账号和日期去重，学习期间不打扰；明确关闭网页后不支持后台推送 |
| 密钥 | 异常后按钮不恢复，清除缺少确认 | 保存与清除失败可重试，清除有确认；修改普通设置仍保留密钥和其他配置 |
| 卡片盒 / 卡片 | 依赖长按发现操作、无恢复暂停入口；弹窗提前关闭 | 明确的更多按钮、收藏和暂停切换、移动与删除确认；请求期间禁用重复操作，失败保留弹窗 |
| 卡片移动 | 刚移走的旧行短暂可再次操作 | 成功后立即更新本地列表；服务端删除、收藏、暂停同时校验所属卡片盒，阻止旧行操作 |
| 社区 | 排序不提交、发现 / 换一批回到同一内容，图标语义不符 | 选择即排序、发现滚动到列表、真实分批切换；搜索图标聚焦输入框；目录可展开预览，已加入卡册进入对应卡片盒 |
| 我的 / 指南 / 统计 | 通知无响应、未实现会员开通、已登录用户打不开指南 | 通知跳转提醒设置；会员占位替换为学习偏好；指南支持步骤按钮并返回我的；热力图可点选日期 |
| AI 结果 | 重试不恢复轮询、删除失败仍移除、保存失败关闭编辑 | 重试恢复进度；失败保留卡片与输入；导入可重试，底部按钮避开主导航 |
| 通用操作 | 缺少按下反馈与请求状态，断网后难恢复 | 140 ms 按下反馈、短内容进入动画、导航等待提示、减少动态效果适配、页面错误重试；新建、编辑、上传、反馈和登录均处理失败状态 |

## 实测与数据边界

- `scripts/interaction-audit.mjs`：57 项通过。含真实收藏 / 暂停 / 恢复 / 移动 / 删除 / 编辑、失败重试、设置校验、AI 重试与失败恢复、320 / 393 / 430 / 1280 px 溢出检查、按下反馈和减少动态效果。
- `scripts/interaction-supplement.mjs`：本地生产构建 14 项通过。含不支持朗读、提醒触发和去重、章节预览、已加入卡册跳转、新建与删除卡片盒、指南、登录失败恢复与退出。
- 两轮浏览器检查均无未捕获的客户端异常。已检查学习菜单和小屏页面截图。
- 设置、密钥、反馈及 AI 生成 / 重试接口在浏览器测试中使用拦截响应；未发送测试反馈，未触发真实模型调用，也未覆盖用户设置和密钥。
- 会写数据库的测试只使用本次创建的临时卡片盒、卡片和 AI 草稿，`finally` 中按账号及 ID / 唯一名称删除并回读确认；成功与失败测试均完成清理。
- 90 项单元测试通过，包含参数边界、卡片队列、挖空朗读、提醒去重、设置并发与密钥保护、移动后旧行删除保护。
- TypeScript 通过，ESLint 0 错误（4 项既有警告），生产构建通过。

截图与机器可读结果位于 `output/playwright/interactions/`。性能回归另见 `output/playwright/performance/regression/results.json`；下方仅记录本轮实际完成的结果。

## 本轮性能回归

`PERF_BASE_URL=http://localhost:3102 node scripts/performance-regression.mjs`：25 项通过，800 张临时卡片，手机模拟和 CPU 降速 4 倍。

- 连续三次评分后界面恢复可操作：132 / 97 / 83 ms；后台请求延迟并失败时仍可继续操作，随后恢复全部未确认评分。数据库回读确认没有写入测试评分。
- 离线卡片盒筛选 48 ms。首批列表不超过 120 行，全部 800 张均可通过滚动或搜索访问。
- 真实 CDP 横向触摸后页面纵向位移为 0，维持卡片内阅读与页面固定的边界。
- 弱网样本（150 ms 延迟、1.6 Mbps、CPU 降速 4 倍）：设置 LCP 1.13 s，今日 0.80 s，编辑器 1.07 s，统计 1.06 s，AI 新建 1.05 s。设置 CLS 0.0051，其余上述样本为 0。系统网页字体下载仍为 0。
- 学习入口本次点击到队列可操作为 1.19 s。预取检测改为收到成功响应后点击，避免等待 Next 流式预取连接结束导致测试挂起；与旧报告等待完整响应后的 310 ms 口径不同，不据此宣称所有导航低于 100 ms。
- 首次性能脚本在等待流结束处挂起，停止该测试进程后按已记录的卡片盒 UUID 清理并回读为 0；修正等待条件后完整回归通过并自动清理。最终再次查询，E2E / 本轮交互 / 性能测试卡片盒及本轮 AI 测试任务残留均为 0。

这些是本地 Chrome 模拟与单次网络样本，不是线上真实用户 INP 或实体手机保证。本轮总计 96 项浏览器检查、90 项单元测试通过。
